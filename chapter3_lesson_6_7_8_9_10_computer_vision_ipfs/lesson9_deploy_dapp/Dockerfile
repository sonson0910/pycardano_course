# ─── DID Face DApp Backend ───
# FastAPI + MediaPipe + PyCardano
#
# ⚠️ FILE NÀY CHỈ ĐỂ THAM KHẢO!
# Dockerfile chính nằm ở THƯ MỤC GỐC (../Dockerfile)
# vì Docker COPY không hỗ trợ đường dẫn "../"
#
# Build từ thư mục gốc:
#   cd chapter3_lesson_6_7_8_9_10_computer_vision_ipfs
#   docker build -t did-face-dapp .
#   docker run -p 8000:8000 --env-file .env did-face-dapp
#
# Xem ../Dockerfile để biết cấu hình đầy đủ.
